// background.js - placeholder for future enhancements
// Manifest v3 requires a service worker if you plan background tasks later.
// For now we won't do anything here.
self.addEventListener('install', () => {
  // noop
});
